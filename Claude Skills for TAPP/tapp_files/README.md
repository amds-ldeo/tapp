# tapp_files

`Template TAPP Group 1 (SUPERSEDED 2026-08-08).csv` is retained for reference only.

Group 1 is now composed from `../modules/Module_Group1.csv` in every TAPP (Rule 6 in
`../references/conventions.md`). The retired template's descriptions were reconciled into
that module on 2026-08-08 — in several fields the better text came from a TAPP rather than
from the template, so the two are not interchangeable.

Do not build a new TAPP's Group 1 from this file.
